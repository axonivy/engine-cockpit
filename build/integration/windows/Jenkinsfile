pipeline {
  agent {
    label 'windows'
  }
  options {
    buildDiscarder(logRotator(numToKeepStr: '60', artifactNumToKeepStr: '5'))
  }

   tools {
    jdk 'temurin-jdk-25.0.2.10'
    maven '3.9'
  }

  triggers {
    cron '@midnight'
  }

  parameters {
    string(name: 'engineSource', defaultValue: 'https://jenkins.ivyteam.io/job/core_product/job/pf-15/', description: 'Engine page url')
  }

  stages {
    stage('WebTestOs') {
      steps {
        script {
          def common = load 'build/build.groovy'
          common.buildIntegrationOnWindows();
          common.recordMavenIssues();
        }
      }
    }
  }
}
