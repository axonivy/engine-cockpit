pipeline {
  agent any

  options {
    buildDiscarder(logRotator(numToKeepStr: '60', artifactNumToKeepStr: '5'))
  }

  triggers {
    cron '@midnight'
  }

  parameters {
    string(name: 'engineSource', defaultValue: 'https://product.ivyteam.io', description: 'Engine page url')
  }

  stages {
    stage('screenshot') {
      steps {
        script {
          def common = load 'build/build.groovy'
          def phase = isReleaseOrMasterBranch() ? 'deploy' : 'verify'
          common.buildScreenshots(phase, getScreenshotRefBranch(), getImageSimilarity())
        }
      }
    }
  }
}

def getScreenshotRefBranch() {
  if (isReleaseOrMasterBranch()) {
    return env.BRANCH_NAME.replace('/', '%252F')
  }
  return 'master'.replace('/', '%252F')
}

def getImageSimilarity() {
  if (isReleaseOrMasterBranch()) {
    return '97'
  }
  return '95'
}

def isReleaseOrMasterBranch() {
  return env.BRANCH_NAME == 'master' || env.BRANCH_NAME.startsWith('release/')
}
